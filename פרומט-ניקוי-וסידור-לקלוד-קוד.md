# פרומט ניקוי וסידור (פשוט) ל‑Claude Code

> פותחים טרמינל בתיקיית הפרויקט, מריצים `claude`, ומדביקים את הטקסט שמתחת לקו.
> מומלץ להריץ קודם את פרומט הפיצ'רים (`פרומט-מפורט-לקלוד-קוד.md`), ואז את הניקוי הזה.

---

שלום. זה פרויקט אתר בית‑ספרי של תלמיד תיכון בתכנות באינטרנט — ASP.NET WebForms, C#, ‎.NET Framework 4.8, עברית RTL, מסד נתונים LocalDB. אני רוצה שתסדר ותנקה את הפרויקט כך שיהיה פשוט, נקי וקל להבנה ולהסבר, ברמה של תלמיד — בלי שום דבר מתקדם.

הדבר הכי חשוב: **אל תשבור את הפרויקט, והוא חייב להמשיך לעבוד בדיוק כמו קודם.** אל תיגע ב‑`Web.config`, במסד הנתונים וב‑`App_Data`, ב‑`Global.asax`, ב‑`packages.config`, ב‑‎`.csproj`, בתיקיות `bin`/`obj`, ובלוגיקה של ההתחברות, ההרשמה, הניהול, ה‑Session, השאילתות וולידציית ה‑JavaScript. אל תוסיף ספריות חיצוניות ואל תשתמש ב‑Bootstrap/Tailwind/React — הכול ברמת תלמיד שיכול להסביר כל שורה. הוסף הערות קצרות בעברית.

מה שאני כן רוצה שתעשה, בזהירות:

1. **לנקות את ה‑CSS.** קובצי ה‑CSS שבשימוש הם `MainStyle.css` ו‑`CourseArea.css`. עבור עליהם ונקה: הסר חוקים כפולים, אחד סלקטורים שחוזרים על עצמם, וסדר שהקוד יהיה קצר וברור. שמור על אותו עיצוב — רק נקי ומסודר יותר.

2. **לעשות עיצוב פשוט וקליל:** רקע בהיר, צבע ראשי אחד, כפתורים פשוטים עם אפקט hover עדין, טפסים נקיים, כותרות ברורות, גבולות רכים לטבלאות, וריווח אחיד. בלי הרבה צבעים, בלי אנימציות, ובלי טריקים מתקדמים ב‑CSS.

3. **למחוק רק קבצים מיותרים לגמרי** (CSS/JS/תמונות/דפים) שלא מקושרים בשום מקום ולא נדרשים — ורק אם אתה בטוח ב‑100% שהם לא בשימוש ושהפרויקט נבנה אחרי המחיקה. אם יש ספק — אל תמחק, פשוט כתוב לי "צריך בדיקה ידנית". שים לב: קובצי Bootstrap/jQuery שמגיעים מ‑NuGet רשומים ב‑‎`.csproj`, אז אל תמחק אותם — רק ציין שהם לא בשימוש וצריך בדיקה ידנית.

4. **לתת שם פשוט לקובץ עם שם מבלבל/זמני** (כמו copy/test/old), ולעדכן את ההפניות אליו. אם השם כבר פשוט וברור — אל תיגע בו.

בסוף: בנה את הפרויקט וודא שאין שגיאות, הרץ ובדוק שהכול עדיין עובד (דף הבית נפתח, הניווט עובד, הרשמה, התחברות, אזור מנהל מוגן, והעיצוב נטען), וכתוב לי סיכום קצר ופשוט של מה שינית ומה מחקת. אם משהו לא בטוח — אל תנחש, פשוט כתוב לי שצריך לבדוק ידנית.
